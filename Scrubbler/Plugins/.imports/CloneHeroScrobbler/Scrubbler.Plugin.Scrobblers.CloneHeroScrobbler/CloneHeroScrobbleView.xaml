<UserControl
    x:Class="Scrubbler.Plugin.Scrobblers.CloneHeroScrobbler.CloneHeroScrobbleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:MediaPlayerScrobblerBase"
    mc:Ignorable="d">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Padding="12,12,12,0" ColumnSpacing="8" RowSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Clone Hero folder" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1"
                     Text="{Binding DataFolderPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"/>
            <Button Grid.Column="2"
                    Content="Select currentsong.txt"
                    Command="{Binding SelectCurrentSongFileCommand}"
                    VerticalAlignment="Center"/>

            <NumberBox Grid.Column="3"
                       Header="Scrobble after"
                       Minimum="30"
                       Maximum="240"
                       SmallChange="1"
                       LargeChange="10"
                       SpinButtonPlacementMode="Compact"
                       Value="{Binding ScrobbleThresholdSeconds, Mode=TwoWay}"/>

            <TextBlock Grid.Row="1"
                       Grid.ColumnSpan="4"
                       Text="{Binding StatusMessage}"
                       Opacity="0.7"/>
        </Grid>

        <media:MediaPlayerScrobblerControl Grid.Row="1" />
    </Grid>
</UserControl>
